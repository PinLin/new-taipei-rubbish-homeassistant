update:
  name: Refresh truck data
  description: Manually refresh garbage truck location data.
  fields:
    point_ids:
      name: Point IDs
      description: Collection point IDs to refresh. Leave empty to refresh all points.
      required: false
      example: ["25.01234_121.45678"]
      selector:
        text:
          multiple: true
